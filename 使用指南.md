# 博客使用指南

## 📝 一、如何添加新文章

### 方法一：在本地添加（推荐）

1. **创建新文件**
   - 在 `posts` 目录下创建一个新的 `.md` 文件
   - 文件名用英文，比如：`my-new-post.md`

2. **编写文章**
   ```markdown
   ---
   title: "文章标题"
   date: "2024-11-23"
   excerpt: "这是文章摘要，会显示在首页列表中"
   readTime: "5 分钟"
   ---

   # 文章正文开始

   这里写你的文章内容...

   ## 二级标题

   可以使用 Markdown 语法：

   - 列表项 1
   - 列表项 2

   **加粗文字**

   *斜体文字*

   [链接文字](https://example.com)
   ```

3. **查看效果**
   - 保存文件后，刷新浏览器
   - 新文章会自动出现在首页

4. **发布到线上**
   ```bash
   git add .
   git commit -m "添加新文章：文章标题"
   git push
   ```
   - 推送后，Vercel 会自动部署（1-2分钟）

### 方法二：在 GitHub 上直接编辑

1. 打开你的仓库：https://github.com/clone668/huxi-blog
2. 进入 `posts` 目录
3. 点击 "Add file" → "Create new file"
4. 文件名输入：`文章名.md`
5. 编写内容（格式同上）
6. 点击 "Commit changes"
7. Vercel 会自动部署

---

## ⚙️ 二、修改博客设置

### ✨ 使用配置文件（推荐）

**所有博客信息都在 `config.ts` 文件中集中管理！**

打开 `config.ts`，修改：

```typescript
export const blogConfig = {
  // 基本信息
  title: '你的博客名称',              // 博客标题
  description: '你的博客描述',         // 博客描述
  author: '你的名字',                 // 作者
  
  // 社交链接
  social: [
    {
      name: 'GitHub',
      url: 'https://github.com/你的用户名',
      type: 'external'
    },
    // 添加更多链接...
  ],
  
  // 导航菜单
  navigation: [
    { name: 'HOME', url: '/' },
    { name: 'ABOUT', url: '/about' },
  ],
  
  // 页脚
  footer: {
    copyright: '© 2025 你的名字',
  },
}
```

**详细说明请查看 `配置说明.md` 文件！**

### 修改关于页面

编辑 `app/about/page.tsx`，修改个人介绍：

```typescript
<p style={{ marginBottom: '15px' }}>
  这里写你的个人介绍...           // ← 修改这里
</p>
```

---

## 🎨 三、自定义样式

### 调整页面宽度

编辑 `app/layout.tsx` 第 18 行：

```typescript
<div style={{ maxWidth: '650px', ... }}>
                      // ↑ 改成 '800px' 会更宽
```

### 调整字体大小

编辑 `app/globals.css`：

```css
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  color: #000;
  background: #fff;
  line-height: 1.6;        /* 行高，可以改成 1.8 更疏松 */
  font-size: 16px;         /* 添加这行，调整全局字体大小 */
}
```

### 修改颜色

**修改链接颜色** - 编辑 `app/globals.css`：
```css
a {
  color: #000;            /* 链接颜色，可以改成 #0066cc 蓝色 */
  text-decoration: underline;
}
```

**修改分隔线颜色** - 编辑对应文件：
- 页眉/页脚的线：在 `app/layout.tsx` 中搜索 `borderBottom: '1px solid #000'`
- 文章间的线：在 `app/page.tsx` 中搜索 `borderBottom: '1px solid #ddd'`

### 调整间距

**文章列表间距** - 编辑 `app/page.tsx` 第 10 行：
```typescript
style={{ marginBottom: '20px', paddingBottom: '20px', ... }}
           // ↑ 改成 '15px' 会更紧凑，'30px' 会更宽松
```

---

## 🚀 四、部署和更新

### 本地开发

```bash
# 启动开发服务器
npm run dev

# 在浏览器打开 http://localhost:3000
```

### 发布更新到线上

```bash
# 1. 添加所有改动
git add .

# 2. 提交改动（填写说明）
git commit -m "修改了什么内容"

# 3. 推送到 GitHub
git push

# 4. 等待 1-2 分钟，Vercel 自动部署完成
```

### 查看部署状态

1. 访问 https://vercel.com
2. 登录后查看项目
3. 可以看到部署历史和状态

---

## 📋 五、日常使用流程

### 发布新文章的完整流程

1. **写文章**
   - 在 `posts` 目录创建 `文章名.md`
   - 按照模板填写元数据和内容

2. **本地预览**
   ```bash
   npm run dev
   ```
   - 打开 http://localhost:3000 查看效果

3. **提交并发布**
   ```bash
   git add .
   git commit -m "添加新文章：文章标题"
   git push
   ```

4. **等待部署**
   - 1-2 分钟后访问你的博客网址
   - 新文章已经上线！

### 修改已有文章

1. 找到 `posts` 目录下对应的 `.md` 文件
2. 直接编辑内容
3. 保存后提交：
   ```bash
   git add .
   git commit -m "更新文章：文章标题"
   git push
   ```

### 删除文章

1. 删除 `posts` 目录下对应的 `.md` 文件
2. 提交：
   ```bash
   git add .
   git commit -m "删除文章：文章标题"
   git push
   ```

---

## 💡 六、常见问题

### Q: 文章不显示怎么办？

**检查文件格式：**
- 文件必须是 `.md` 结尾
- 必须有正确的元数据（title、date、excerpt）
- 元数据用 `---` 包裹

示例：
```markdown
---
title: "标题"
date: "2024-11-23"
excerpt: "摘要"
---

正文内容...
```

### Q: 推送到 GitHub 失败？

```bash
# 如果提示需要登录，设置凭据
git config --global credential.helper store

# 再次推送
git push
```

### Q: 本地修改后浏览器没变化？

- 硬刷新浏览器：`Ctrl + Shift + R`（Windows）
- 或者清除缓存后刷新

### Q: Vercel 部署失败？

1. 登录 Vercel 查看部署日志
2. 检查是否有语法错误
3. 确保本地 `npm run build` 能成功运行

### Q: 如何绑定自己的域名？

1. 在 Vercel 项目设置中选择 "Domains"
2. 添加你的域名
3. 按照提示配置 DNS 记录
4. 等待生效（几分钟到几小时）

---

## 📚 七、Markdown 语法速查

```markdown
# 一级标题
## 二级标题
### 三级标题

**加粗文字**
*斜体文字*

[链接文字](https://example.com)

![图片描述](图片链接)

- 无序列表项
- 无序列表项

1. 有序列表项
2. 有序列表项

> 引用文字

`行内代码`

​```
代码块
​```
```

---

## 📁 八、重要文件说明

```
blog/
├── config.ts          ← 📌 博客配置文件（修改博客信息）
├── posts/             ← 📝 所有文章存放这里
├── app/
│   ├── layout.tsx     ← 页面布局（一般不需要改）
│   ├── page.tsx       ← 首页
│   ├── about/
│   │   └── page.tsx   ← 关于页面
│   └── globals.css    ← 全局样式
├── 使用指南.md        ← 使用说明
└── 配置说明.md        ← 配置文件详细说明
```

---

## 🎯 九、推荐工作流程

### 周期性写作

1. **每周固定时间**写一篇文章
2. **在本地预览**确认效果
3. **推送发布**
4. 在社交媒体分享

### 使用草稿

可以在 `posts` 目录创建 `drafts` 子目录存放草稿：
```
posts/
├── welcome.md          (已发布)
├── my-post.md         (已发布)
└── drafts/
    └── draft-post.md  (草稿，不会显示)
```

注意：只有直接在 `posts` 目录下的 `.md` 文件才会显示

---

## 📞 十、获取帮助

- **Next.js 文档**: https://nextjs.org/docs
- **Markdown 指南**: https://www.markdownguide.org/
- **Vercel 文档**: https://vercel.com/docs
- **Git 教程**: https://git-scm.com/book/zh/v2

---

开始享受写作吧！🎉
