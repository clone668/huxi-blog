# 博客配置说明

## 📋 配置文件位置

所有博客信息都在 **`config.ts`** 文件中集中管理。

## ⚙️ 如何修改配置

打开 `config.ts` 文件，你会看到：

```typescript
export const blogConfig = {
  // 基本信息
  title: 'Huxi Blog',              // 博客名称
  description: '分享技术、生活和思考',  // 博客描述
  author: 'Huxi',                  // 作者名
  
  // 社交链接
  social: [
    // ...链接列表
  ],
  
  // 导航菜单
  navigation: [
    // ...菜单项
  ],
  
  // 页脚信息
  footer: {
    copyright: '© 2025',           // 版权信息
  },
}
```

---

## 🎯 常见修改

### 1. 修改博客名称和描述

```typescript
title: '你的博客名称',
description: '你的博客描述',
author: '你的名字',
logo: '✨',  // 博客图标，可以是 emoji 或图片路径
```

**Logo 使用说明**：

**方式一：使用 Emoji（推荐）**
```typescript
logo: '✨',   // 闪光
logo: '📝',   // 笔记本
logo: '🌟',   // 星星
logo: '💡',   // 灯泡
logo: '🚀',   // 火箭
logo: '📚',   // 书籍
```

**方式二：使用图片**
```typescript
logo: '/logo.png',  // 图片放在 public 目录
```
然后将你的 logo.png 文件放到 `public` 目录下。

**隐藏 Logo**
```typescript
logo: '',  // 留空即不显示
```

### 2. 添加/修改社交链接

```typescript
social: [
  {
    name: 'GitHub',                              // 显示名称
    url: 'https://github.com/你的用户名',         // 链接地址
    type: 'external'                             // 类型：external 或 email
  },
  {
    name: 'Twitter',
    url: 'https://twitter.com/你的用户名',
    type: 'external'
  },
  {
    name: 'Email',
    url: 'mailto:你的邮箱@example.com',
    type: 'email'
  },
],
```

#### 添加更多链接

只需在数组中添加新项：

```typescript
social: [
  // 已有的链接...
  {
    name: 'Telegram',
    url: 'https://t.me/你的用户名',
    type: 'external'
  },
  {
    name: '知乎',
    url: 'https://zhihu.com/people/你的用户名',
    type: 'external'
  },
  {
    name: 'RSS',
    url: '/rss.xml',
    type: 'external'
  },
],
```

#### 删除链接

直接删除对应的对象即可。

### 3. 修改导航菜单

```typescript
navigation: [
  {
    name: 'HOME',      // 显示名称
    url: '/',          // 链接地址
  },
  {
    name: 'ABOUT',
    url: '/about',
  },
  // 如果以后添加新页面，可以在这里加
  {
    name: 'ARCHIVE',
    url: '/archive',
  },
],
```

### 4. 修改版权信息

```typescript
footer: {
  copyright: '© 2025 你的名字',
},
```

### 5. 设置每页显示文章数

```typescript
pagination: {
  postsPerPage: 10,  // 改成 5 = 每页显示5篇，改成 20 = 每页显示20篇
},
```

---

## 📝 链接类型说明

- **`type: 'external'`** - 外部链接，会在新窗口打开
- **`type: 'email'`** - 邮件链接，点击会打开邮件客户端

---

## 🔄 应用更改

修改配置后：

1. **保存文件** `config.ts`
2. **刷新浏览器**（本地开发时）
3. **推送到 GitHub**（部署到线上）
   ```bash
   git add .
   git commit -m "更新配置"
   git push
   ```

---

## 💡 优势

✅ **一处修改，全站生效**
- 左侧栏（桌面端）自动更新
- 顶部导航（移动端）自动更新
- 页面标题自动更新

✅ **不需要找文件**
- 所有配置集中在一个文件
- 清晰的注释说明

✅ **易于维护**
- 添加/删除链接很简单
- 不会改错其他代码

---

## 🎨 完整配置示例

```typescript
export const blogConfig = {
  title: '张三的博客',
  description: '记录编程、旅行和生活',
  author: '张三',
  
  social: [
    {
      name: 'GitHub',
      url: 'https://github.com/zhangsan',
      type: 'external'
    },
    {
      name: '微博',
      url: 'https://weibo.com/zhangsan',
      type: 'external'
    },
    {
      name: 'Email',
      url: 'mailto:zhangsan@example.com',
      type: 'email'
    },
    {
      name: 'RSS',
      url: '/rss.xml',
      type: 'external'
    },
  ],
  
  navigation: [
    {
      name: '首页',
      url: '/',
    },
    {
      name: '关于',
      url: '/about',
    },
    {
      name: '归档',
      url: '/archive',
    },
  ],
  
  footer: {
    copyright: '© 2025 张三',
  },
}
```

---

## ❓ 常见问题

**Q: 修改后没有生效？**
- 确保保存了 `config.ts` 文件
- 刷新浏览器（Ctrl + Shift + R 强制刷新）

**Q: 可以添加图标吗？**
- 目前是极简风格，只有文字链接
- 如需图标，需要额外修改代码

**Q: 可以改顺序吗？**
- 可以！直接在数组中调整对象的顺序即可

---

开始自定义你的博客吧！🎉
